{
  "name": "generation-league",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "vite --host 127.0.0.1",
    "server": "node server/world-server.mjs",
    "build": "tsc -b && vite build",
    "assets:validate": "node scripts/validate-creature-assets.mjs",
    "check": "npm run assets:validate && tsc -b --pretty false && vitest run",
    "test": "vitest run",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "pg": "^8.22.0",
    "phaser": "4.2.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "7.1.12",
    "vitest": "3.2.4"
  }
}
